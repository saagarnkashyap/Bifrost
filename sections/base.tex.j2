% AUTO-GENERATED by build_resume.py. Edit sections/*.yaml, not this file.
\documentclass[<< page.fontsize >>,letterpaper]{article}
\usepackage[margin=<< page.margin >>]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}   % ATS: Type1 font so pdftotext reads 'finance', not 'nance'
\usepackage[hidelinks]{hyperref}

% ATS: make ligatures (fi, ff) extract as real letters
\input{glyphtounicode}
\pdfgentounicode=1

\pagestyle{empty}
\setlength{\parindent}{0pt}
\raggedright
\raggedbottom

% --- section heading: name + rule -------------------------------------
\newlength{\rsecskip}\setlength{\rsecskip}{<< page.section_spacing | default('5pt') >>}
\newcommand{\rsection}[1]{%
  \vspace{\rsecskip}{\large\scshape #1}\par\vspace{1pt}\hrule\vspace{3pt}}

% --- tight bullet list -------------------------------------------------
\newenvironment{rbullets}
  {\begin{list}{$\bullet$}{%
     \setlength{\leftmargin}{1.5em}\setlength{\labelwidth}{1em}%
     \setlength{\itemsep}{1pt}\setlength{\parsep}{0pt}%
     \setlength{\topsep}{2pt}\setlength{\partopsep}{0pt}}\small}
  {\end{list}}

% --- entry header: left bold / right aligned date -----------------------
\newcommand{\rheading}[2]{%
  \textbf{#1}\hfill #2\par}
\newcommand{\rsubheading}[2]{%
  \textit{\small #1}\hfill \textit{\small #2}\par\vspace{2pt}}

\begin{document}

\begin{center}
  {\LARGE\bfseries << b.name >>}\\[4pt]
  \small << b.location >> $|$ << b.phone >> $|$
  \href{mailto:<< b.email >>}{<< b.email >>}
%% for l in b.links
  $|$ \href{<< l.url >>}{<< l.label >>}
%% endfor
\end{center}
\vspace{2pt}

%% for sec in sections
%% include 'templates/' ~ sec ~ '.tex.j2'
%% endfor

\end{document}
